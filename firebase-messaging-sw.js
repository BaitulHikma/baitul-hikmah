// Firebase Cloud Messaging Background Service Worker Entry Point
// Imports the main service worker so background push notifications work reliably
// even when the app/browser is closed.
importScripts('./sw.js');
